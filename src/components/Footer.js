import React from "react"
import HeaderStyles from "./CVForm/styles/HeaderStyles"

const Footer = (props) => {
    return (
      <HeaderStyles.Header>
        This is the footer 
      </HeaderStyles.Header>
    );
}

export default Footer