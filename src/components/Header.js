import React from "react";
import HeaderStyles from "./CVForm/styles/HeaderStyles"

const Header = (props) => {
    return (
      <HeaderStyles.Header>
        This is a header
      </HeaderStyles.Header>
    );
}

export default Header;