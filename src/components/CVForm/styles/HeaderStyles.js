import styled from "styled-components"

const Header = styled.div`
  background: #03045e;
  color: white;
  font-size: 2em;
  padding: 0.3em;
`;

const HeaderStyles = {
    Header: Header,
}

export default HeaderStyles